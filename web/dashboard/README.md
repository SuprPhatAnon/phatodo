# Dashboard

The dashboard will provide a browser UI for projects, epics, tasks, comments, dependencies, search, and audit history.

Initial server scaffolding renders a placeholder page from `internal/server`. When a frontend stack is selected, place dashboard source here and serve built assets from the API server.
